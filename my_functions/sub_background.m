function diff_image = sub_background(image1, image2)
    %Given two images, this function attempts to subtract common pixels
    % from both images, returning an image with the differing pixels.
    %
    %image1 is usually a background image and image2 is the one containing
    %objects you are after
   
    %TO BE COMPLETED
end